# Credentials

- Demo app logins (portal / mobile / reporter): **`credentials.json`** at repo root (tracked).
- Runtime secrets (DB, AWS, Flask) stay in **`.env`** — never commit `.env`.
- Do not invent new default passwords or re-seed users unless asked.
- Do not print the full credentials file unless the user asks for logins.
