# Maps: OpenStreetMap only — no Google Maps key needed (see src/components/OsmMap.js).

# API base URL by build type (see src/api.js)
# Dev / Expo Go / expo start -> EXPO_PUBLIC_API_BASE_DEV
#   Physical phone on Wi-Fi: your PC LAN IP, e.g. http://192.168.1.42:5000
#   Android emulator:        http://10.0.2.2:5000  (host machine localhost)
# Release APK / production  -> EXPO_PUBLIC_API_BASE_PROD
EXPO_PUBLIC_API_BASE_DEV=http://10.0.2.2:5000
EXPO_PUBLIC_API_BASE_PROD=http://45.194.2.247:5005
